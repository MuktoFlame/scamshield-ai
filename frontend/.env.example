# Copy to .env for production builds. Leave empty in development — the Vite
# dev server proxies /api to http://127.0.0.1:8000 automatically.

# Base URL of the deployed backend, e.g. https://scamshield-api.onrender.com
VITE_API_URL=
